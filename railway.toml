[build]
builder = "nixpacks"

buildCommand = "apt-get install -y libasound2t64 libgbm1 libnss3 libxss1 libatk-bridge2.0-0 libgtk-3-0 2>/dev/null || true && playwright install chromium"
[deploy]
startCommand = "python bot.py"
restartPolicyType = "always"
